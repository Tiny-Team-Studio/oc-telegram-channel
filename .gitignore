node_modules/
.env
channel.env
state/
*.local

# oc-schedule is cloned alongside on the server as a file: dependency;
# never commit it into this repo (match both the dir and the local dev symlink)
oc-schedule
oc-schedule/
